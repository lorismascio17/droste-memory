name: Growth Radar

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 1"

jobs:
  radar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Check public discovery state
        run: python scripts/growth_radar.py
